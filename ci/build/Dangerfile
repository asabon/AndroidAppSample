if File.exist?("build-report.md")
  report = File.read("build-report.md").strip
  if report.empty?
    message("✅ ビルドレポートは空です。")
  else
    markdown("### 📊 ビルドレポート\n\n#{report}")
  end
else
  fail("❌ `build-report.md` が存在しません。")
end
